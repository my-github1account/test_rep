name: Build and publish image from fork
on:
  issue_comment:
    types: created

jobs:
  publish:
    name: Publish images
    if: ${{github.event.issue.pull_request != null}} && ${{ startsWith(github.event.comment.body, '/builds') }} 
    runs-on: ubuntu-22.04
    steps:
      - name: ECHO
        run: |
          echo "github.event.issue.pull_request != null ${{github.event.issue.pull_request != null}}"
          echo "startsWith(github.event.comment.body, '/builds') ${{ startsWith(github.event.comment.body, '/builds') }}"
